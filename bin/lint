#!/bin/sh

set -e

echo "===> Running elm-format"
elm-format --validate web/src
echo "===> elm-format satisfied"

echo "===> Running rubocop"
cd api && bundle exec rubocop && cd ..
echo "===> rubocop satisfied"
